CREATE TABLE employees (
    emp_id INT PRIMARY KEY,
    name VARCHAR(100),
    age INT,
    gender VARCHAR(10),
    department VARCHAR(50),
    role VARCHAR(50),
    salary NUMERIC,
    performance_score INT,
    join_date DATE,
    exit_date DATE,
    location VARCHAR(50)
);
